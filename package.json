{
  "name": "soul-sanctuary",
  "version": "1.0.0",
  "description": "SOuL Sanctuary Web Application",
  "main": "server/src/websocket/WebSocketServer.js",
  "scripts": {
    "start": "node server/src/websocket/WebSocketServer.js",
    "build": "cd app && npm install --legacy-peer-deps && npm run build",
    "dev": "nodemon server/src/websocket/WebSocketServer.js",
    "postinstall": "cd app && npm install --legacy-peer-deps"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "express": "^4.17.1",
    "ws": "^8.2.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
